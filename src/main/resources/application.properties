# this connects us to the database
spring.datasource.url = jdbc:mysql://localhost:3306/pma?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
spring.datasource.username = root
spring.datasource.password = OZuzzFA^qkfHKJkU9u=8v4=eS+Jt/8%

# this will show the SQL in the console
spring.jpa.show-sql = true

# This will turn off cache on the server... only use for development. This should be true for production
spring.thymeleaf.cache = false

# Hibernate properties
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

# Hibernate auto ddl ... options are create, create-drop, validate, none. do not want to create in a production enviroment
spring.jpa.hibernate.ddl-auto = none
#change to update after the building phase is done

# this allows you to see the queries
logging.level.org.hibernate.sql = DEBUG

# this will run if you have a sql schema or .sql file. this will run by default if not specified.
spring.sql.init.mode= never

# this prevents any sql errors 
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation = true